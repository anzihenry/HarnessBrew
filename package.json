{
  "name": "harnessbrew",
  "version": "0.3.0",
  "description": "A personal asset library and workflow package manager for coding agents.",
  "type": "module",
  "bin": {
    "harnessbrew": "./src/cli.js"
  },
  "scripts": {
    "start": "node ./src/cli.js",
    "test": "node --test ./tests/*.test.js",
    "check": "node --check ./src/cli.js && node --check ./src/core/workspace.js && node --check ./src/core/adapters.js && node --check ./src/core/paths.js && node --check ./src/utils/diff.js && node --check ./src/utils/json.js && npm run test"
  },
  "keywords": [
    "agent",
    "assets",
    "skills",
    "instructions",
    "versioning"
  ],
  "license": "MIT"
}
